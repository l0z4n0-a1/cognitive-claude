{
  "_comment": "Template para projetos simples. Drop in <repo>/.claude/settings.json. Adapt deny patterns to your stack.",
  "permissions": {
    "deny": [
      "Bash(rm -rf *)",
      "Bash(git push --force*)",
      "Bash(git push -f *)",
      "Bash(*--no-verify*)",
      "Bash(git reset --hard*)",
      "Read(./.env)",
      "Read(./.env.*)"
    ]
  }
}
